<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.strnd.api.service.ServiceMapper">

    <select id="findAllActive" resultType="com.strnd.api.service.domain.Service">
        <!-- 활성 서비스 목록 조회 -->
        SELECT
            SERVICE_ID, SERVICE_CODE, SERVICE_NAME, IS_ACTIVE, SORT_ORDER
        FROM mg_services
        WHERE IS_ACTIVE = 1
        ORDER BY SORT_ORDER ASC
    </select>

    <select id="findById" resultType="com.strnd.api.service.domain.Service">
        <!-- 서비스 ID로 단건 조회 -->
        SELECT SERVICE_ID, SERVICE_CODE, SERVICE_NAME, IS_ACTIVE, SORT_ORDER
        FROM mg_services
        WHERE SERVICE_ID = #{serviceId}
    </select>

</mapper>