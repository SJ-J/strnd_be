<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.strnd.api.home.HomeMapper">

    <select id="countMonthlyVisits" parameterType="Long" resultType="int">
        <!-- 이번 달 방문 수 (VISIT_DT 기준) -->
        SELECT COUNT(*)
        FROM tb_visit_record
        WHERE DESIGNER_ID = #{designerId}
        AND YEAR(VISIT_DT) = YEAR(NOW())
        AND MONTH(VISIT_DT) = MONTH(NOW())
    </select>

    <select id="findCustomers" resultType="com.strnd.api.customer.domain.Customer">
        <!-- 고객 목록 조회 (LAST_VISIT_DT 내림차순, NULL 후순위 / limit 지정 시 상위 N건) -->
        SELECT
            CUSTOMER_ID, CUSTOMER_NAME, PHONE, LAST_VISIT_DT
        FROM tb_customers
        WHERE DESIGNER_ID = #{designerId}
        ORDER BY LAST_VISIT_DT IS NULL ASC, LAST_VISIT_DT DESC
        <if test="limit != null">
            LIMIT #{limit}
        </if>
    </select>

</mapper>