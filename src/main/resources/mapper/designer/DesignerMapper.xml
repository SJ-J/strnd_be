<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.strnd.api.designer.DesignerMapper">

    <select id="findByName" parameterType="String" resultType="com.strnd.api.designer.domain.Designer">
        <!-- 디자이너명으로 디자이너 단건 조회 -->
        SELECT DESIGNER_ID, DESIGNER_NAME, PIN_HASH, IS_ACTIVE, PHONE, LAST_LOGIN_DT, REG_DT, MOD_DT
        FROM mg_designers
        WHERE DESIGNER_NAME = #{designerName}
    </select>

    <select id="findByPhone" parameterType="String" resultType="com.strnd.api.designer.domain.Designer">
        <!-- 연락처로 디자이너 단건 조회 -->
        SELECT DESIGNER_ID, DESIGNER_NAME, PIN_HASH, IS_ACTIVE, PHONE, LAST_LOGIN_DT, REG_DT, MOD_DT
        FROM mg_designers
        WHERE PHONE = #{phone}
    </select>

    <insert id="insert" parameterType="com.strnd.api.designer.domain.Designer" useGeneratedKeys="true" keyProperty="designerId">
        <!-- 디자이너 신규 등록 -->
        INSERT INTO mg_designers (DESIGNER_NAME, PHONE, PIN_HASH, IS_ACTIVE)
        VALUES (#{designerName}, #{phone}, #{pinHash}, 1)
    </insert>

    <update id="updateLastLoginDt" parameterType="Long">
        <!-- 로그인 성공 시 마지막 로그인 일시 갱신 -->
        UPDATE mg_designers
        SET LAST_LOGIN_DT = NOW(), MOD_DT = NOW()
        WHERE DESIGNER_ID = #{designerId}
    </update>

</mapper>